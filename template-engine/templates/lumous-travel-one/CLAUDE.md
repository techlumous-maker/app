# CLAUDE.md

Read `Agent.md` in this folder before making any change to this template — it
is the authoritative brief for `lumous-travel-one` (page sections and their
order, CTA behavior, and editable controls).

This file exists only as the Claude-specific pointer to that brief. `Agent.md`
itself intentionally stays plain, assistant-agnostic markdown (no Claude-only
format) so Codex or any other AI assistant can read it directly too.
